토픽, 섭시스 ,액션, 파라미터 4개를 데이터를 주고받고하는데, 이때 자료형 즉 타입이 있어야하는데 이를 인터페이스라 한다. 
구조체- 사용자 정의 자료형
 자료형 종료 -문자열, 불, 정수형, 부동 소수점형,
 커맨드 벨
 특징을 변수만 가지고 전부다 표현할 수 없다. ->즉 변수뿐만아니라 동작을 시키는 함수도 요구된다. 둘다 포함하는걸 class
 int자료형 person 둘다 크게 자료형이라 볼수있다. class int float bool 다 자료형이다. 
 변수만 포함 하는것은 구조체(사용자 정의 자료형)
 자료형 쓰는이유 data type data를 다룰때
 int a->이렇게 작성하는 이유는 컴파일러한테 변수선언 정수만 사용, 데이터 어느정도 사용한다고 미리 메모리 할당.
 node 변수라고도 하지만 객체라고도 함.
대부분의 클래스앞에 생성자라는 함수를 가짐
호출방식 2가지
subcall back함수는 sub를 받는 순간.
timer call back 특정 주기마다 callback되는
 
 
 
 
 단어에대해 이야기 3가지 이미지그림, 문자단어 그자체로, 반대되는 단어
 
